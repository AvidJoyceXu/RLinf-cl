defaults:
  - ../../../reasoning/config/training_backend/fsdp@inference.fsdp_config
  - ../../../reasoning/config/training_backend/fsdp@actor.fsdp_config
  - override hydra/job_logging: stdout


hydra:
  run:
    dir: .
  output_subdir: null


cluster:
  num_nodes: 1
  component_placement:
    actor,rollout,reward: all


runner:
  task_type: reasoning
  logger:
    log_path: ${runner.output_dir}/${runner.experiment_name}
    project_name: rlinf
    experiment_name: ${runner.experiment_name}
    logger_backends: ["tensorboard"]

  max_epochs: 1
  max_steps: -1

  val_check_interval: 25
  save_interval: 25

  seq_length: 8192

  enable_dynamic_batch_size: True
  max_tokens_per_mbs: 11008

  resume_dir: null
  experiment_name: eqa-grpo-qwen2.5vl-7b-m2-smoke
  output_dir: ../results


algorithm:
  group_size: 4
  n_minibatches: 2
  training_batch_size_per_gpu: 1
  rollout_batch_size_per_gpu: null

  logprob_forward_micro_batch_size: 16
  val_rollout_batch_size_per_gpu: 1

  recompute_logprobs: True
  shuffle_rollout: False

  loss_type: actor
  loss_agg_func: "token-mean"
  kl_beta: 0.001
  kl_penalty_type: low_var_kl
  ratio_clip_eps: 0.2
  entropy_bonus: 0.0
  calculate_entropy: False
  clip_ratio_c: null
  clip_ratio_low: null
  clip_ratio_high: null

  # Plain GRPO (FSDP path); the MA-GRPO loss_scales over group/agent/turn rely
  # on MAMegatronActor and have no FSDP analog yet — see train.py note.
  adv_type: grpo
  normalize_advantages: False
  early_stop_imp_ratio: 5.0
  use_valid_token_scale: False

  sampling_params:
    do_sample: True
    temperature: 1.0
    top_k: 1000000
    top_p: 1.0
    repetition_penalty: 1.0
    max_new_tokens: ${subtract:${runner.seq_length}, ${data.max_prompt_length}}
    min_new_tokens: 1
    stop: ["</tool_call>", "<|im_end|>"]


data:
  type: math
  max_prompt_length: 4096
  filter_prompt_by_length: True
  rollout_batch_size: 32
  val_rollout_batch_size: null
  num_workers: 2
  prompt_key: prompt
  answer_key: solutions
  apply_chat_template: False
  shuffle: True
  validation_shuffle: True
  seed: 1234
  train_data_paths: ["/data/hm_eqa/train.jsonl"]


agentloop:
  group_name: "AgentLoopGroup"
  toolcall_parser: "eqa-qwen"
  max_turns: 16
  max_tool_response_length: 800
  tool_response_truncate_side: "right"
  print_outputs: False
  is_dynamic_rollout_batch: True
  scripted_policy: False


tools:
  habitat_eqa:
    pool_size: 1
    render_rgb: False


rollout:
  group_name: "RolloutGroup"
  gpu_memory_utilization: 0.5
  model:
    # HF cache snapshot dir (Qwen/Qwen2.5-VL-7B-Instruct). Override via Hydra
    # if the snapshot hash differs in your cache.
    model_path: /opt/.cache/huggingface/hub/models--Qwen--Qwen2.5-VL-7B-Instruct/snapshots/cc594898137f460bfe9f0759e9844b3ce807cfb5
    model_type: qwen2.5_vl
    precision: ${actor.model.precision}
  enforce_eager: False
  distributed_executor_backend: mp
  disable_log_stats: False
  detokenize: True
  padding: null
  eos: null
  return_logprobs: ${not:${algorithm.recompute_logprobs}}

  rollout_backend: sglang
  sglang:
    attention_backend: triton
    decode_log_interval: 500000
    use_torch_compile: False
    torch_compile_max_bs: 128
  vllm:
    attention_backend: FLASH_ATTN
    enable_chunked_prefill: True
    enable_prefix_caching: True
    enable_flash_infer_sampler: True
    max_num_batched_tokens: null
    torch_profiler_dir: null

  tensor_parallel_size: 1
  pipeline_parallel_size: 1
  max_running_requests: 64
  cuda_graph_max_bs: 64

  validate_weight_first_sync: True
  validate_weight: False
  validate_save_dir: null
  print_outputs: False


reward:
  group_name: "RewardGroup"
  use_reward_model: False
  reward_type: 'eqa'
  reward_scale: 1.0

  tokenizer:
    tokenizer_model: ${actor.tokenizer.tokenizer_model}
    use_fast: ${actor.tokenizer.use_fast}
    trust_remote_code: ${actor.tokenizer.trust_remote_code}
    padding_side: ${actor.tokenizer.padding_side}


actor:
  group_name: "ActorGroup"
  training_backend: fsdp
  enable_dp_load_balance: True
  calculate_flops: False
  seed: 1234

  enable_offload: True

  global_batch_size: 8
  micro_batch_size: 1

  model:
    model_type: ${rollout.model.model_type}
    model_path: ${rollout.model.model_path}
    precision: bf16
    sharding_strategy: full_shard
    is_lora: False

    seq_length: ${runner.seq_length}
    encoder_seq_length: ${runner.seq_length}

  optim:
    optimizer: adam
    bf16: True
    fp16: False
    lr: 1.0e-06
    adam_beta1: 0.9
    adam_beta2: 0.95
    adam_eps: 1.0e-05
    min_lr: 0.0
    weight_decay: 0.01
    use_distributed_optimizer: True
    overlap_grad_reduce: False
    overlap_param_gather: False
    optimizer_enable_pin: False
    overlap_param_gather_with_optimizer_step: False
    clip_grad: 0.8
    loss_scale: 65536

  lr_sched:
    lr_warmup_fraction: 0.05
    lr_warmup_init: 0.0
    lr_warmup_iters: 0
    max_lr: 1.0e-06
    min_lr: 0.0
    lr_decay_style: constant
    lr_decay_iters: 1000

  tokenizer:
    tokenizer_model: ${rollout.model.model_path}
    use_fast: False
    trust_remote_code: True
    padding_side: 'right'


inference:
  model_type: ${rollout.model.model_type}
  group_name: "InferenceGroup"
  load_from_actor: True
  model:
    tensor_model_parallel_size: 1
    pipeline_model_parallel_size: 1
    sequence_parallel: False


critic:
  use_critic_model: False
