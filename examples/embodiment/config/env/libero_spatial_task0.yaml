env_type: libero
task_suite_name: libero_spatial

total_num_envs: null

auto_reset: False
ignore_terminations: False
max_steps_per_rollout_epoch: 240
max_episode_steps: 240 # max episode steps for truncation

use_rel_reward: True
reward_coef: 1.0

# RLinf LiberoEnv specific settings
reset_gripper_open: True
is_eval: False

seed: 0
group_size: 1
use_fixed_reset_state_ids: True
use_ordered_reset_state_ids: False

# Indicate the reset state id (related to the task_id and trial_id) want to be specified. (e.g. 
# if set 0, the env always use the task 0 and trial 0 in the libero-spatial suite; 
# if set null, the initial states will be sampled from the whole libero-spatial suite. )
# For libero_spatial: task 0 uses reset_state_id [0, 50), task 1 uses [50, 100), ..., task 9 uses [450, 500)
# num_trials_per_task: 50 (需要验证)
specific_reset_id: 0
# Limit the number of trials to use from the specified task (only used when specific_reset_id is set)
# Set to 1 to use only trial_id=0 (the first trial) of task_id=0
max_trials_per_task: 20

video_cfg:
  save_video: True
  info_on_video: True
  video_base_dir: ${runner.logger.log_path}/video/train

init_params:
  camera_heights: 256
  camera_widths: 256